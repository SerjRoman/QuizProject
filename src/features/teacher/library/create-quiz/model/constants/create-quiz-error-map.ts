import type { ErrorMap } from "@/shared/lib";

export const CREATE_QUIZ_ERROR_MAP:ErrorMap = {
    409: {
        __DEFAULT__: "Validation error. Check all of yours "
    },
    422: {
        
    }
}